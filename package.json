{
  "name": "misc",
  "version": "1.0.0",
  "description": "Demos, misc",
  "main": "index.js",
  "scripts": {
    "start": "start /b if exist build (rmdir /s/q build) else (mkdir build) & start /b fis release -f ./fis-conf.js -r ./src -wd ./build & http-server ./build -p 8080 -s -c-1",
    "dist": "rmdir /s/q dist & set FIS_DEBUG=FALSE& fis release -f ./fis-conf.js -r ./src -D -opd ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dajbd/misc.git"
  },
  "author": "Engin",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dajbd/misc/issues"
  },
  "homepage": "https://github.com/dajbd/misc#readme",
  "dependencies": {
    "fis": "^1.9.39",
    "fis-lint-csslint": "0.0.2",
    "fis-lint-jshint": "0.0.10",
    "fis-parser-less-2.x": "^0.1.4",
    "fis-postprocessor-cssprefixer": "0.0.3",
    "http-server": "^0.8.5"
  }
}
